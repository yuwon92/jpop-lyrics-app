# Release Notes

## v1.8.4
- **탭 전환 시 작업 상태 유지** — 단어장·문법 노트 등 다른 탭에 다녀와도 가사 번역 화면이 초기화되지 않고 하던 작업 그대로 유지 (노래 목록에서 새 번역·편집을 시작할 때만 초기화)
  - 단어장·문법 노트도 보기 모드, 선택한 노래, 필터가 유지됨
  - 각 화면의 스크롤 위치도 탭을 오갈 때 유지 (앱 재시작 시에는 초기화)
- **설정 창 닫기** — 설정 창 우측 상단 `×` 버튼으로 이전에 보던 탭으로 돌아가기
- **한글 발음 변환 버그 수정** — 영어로만 된 줄이 있으면 이후 줄의 한글 발음이 한 칸씩 밀리던 문제 수정
  - 영어 등 일본어가 아닌 부분은 변환하지 않고 그대로 표시 (일본어와 섞인 줄 포함)

## v1.8.3
- 버그 수정

## v1.8.2
- 버그 수정

## v1.8.1
- 맥용 워크플로우 패치

## v1.8.0
- **기본형 추천 기능** — 가사에서 활용형 단어를 드래그 후 `Ctrl+E`를 누르면 형태소 분석(kuromoji)으로 기본형을 추천
  - 예: `食べました` 선택 → `食べる` 추천, 읽기 `たべる` 자동 입력
  - 모달에서 `원문 사용` ↔ `기본형 추천 사용` 버튼으로 전환 가능, 읽기·뜻도 해당 단어 기준으로 다시 채워짐
  - 사전·AI 없이 로컬에서 동작, 분석 실패 시 기존 단어 추가 흐름 그대로 유지
- **단어 뜻 번역 캐시** — AI 뜻 자동 입력 결과를 로컬 캐시에 저장, 같은 단어 재조회 시 API 재호출 없음 (크레딧 절약)
- **가사 번역 화면 닫기** — 창 우측 상단 `×` 버튼으로 노래 목록으로 돌아가기 (저장 안 된 편집은 자동 저장)
- 다크 테마 가독성 수정 — 기본형 추천 강조 색상, `Ctrl+E로 단어 추가` 힌트 팝업이 다크 테마에서 보이지 않던 문제 수정

## v1.7.6
- **화이트 테마 개선** — 선택 영역 가독성 수정 및 테마 컬러 수정(화이트&옐로)

## v1.7.5
- **성능 개선** — 저장 방식 최적화(연속 변경을 모아 한 번에 기록), 분석 캐시 용량 제한, 가사 편집 화면 렌더링 최적화
- **내부 구조 정리** — 공통 모달 컴포넌트 도입, 중복 코드 제거, 대형 컴포넌트 분리
- **접근성 개선** — 키보드 탐색(포커스 표시·모달 포커스 트랩), 스크린 리더용 버튼 라벨 추가

## v1.7.4
- **API 키 관리** — 설정 화면에서 API 키 등록·변경·삭제 가능
- **가사 분석 개선** — API 키가 없을 때 분석 버튼에서 바로 키 등록 후 이어서 분석
- **보안 강화** — 렌더러 프로세스 격리(sandbox), 프로덕션 CSP 적용, 배포 빌드에서 DevTools 차단
- **오프라인 폰트** — 모든 폰트를 앱에 내장, 인터넷 없이도 화면 깨짐 없음
- 삭제·즐겨찾기 실패 시 오류 표시 추가

## v1.7.3
- **데이터 유실 방지** — 저장 데이터를 더 안전하게 보관
  - 원자적 저장 방식 도입: 저장 도중 앱이 꺼져도 기존 데이터가 손상되지 않음
  - 앱 시작 시 자동 백업(`.bak`) 생성 — 문제가 생겨도 마지막 정상 시점으로 복구 가능
  - 데이터 파일이 손상된 경우 삭제하지 않고 보존한 뒤 복구 방법 안내
- **오류 안내 개선** — 조용히 실패하던 동작들이 이제 화면에 오류를 표시
  - 노래 목록·단어장·문법 노트 불러오기 실패 시 오류 배너 + 다시 시도 버튼
  - 가사 변환·저장 실패 시 오류 표시 (자동 저장 무한 재시도 방지 포함)
  - 히라가나 변환 사전 로딩 실패 시 안내 메시지 표시
  - 예기치 못한 오류로 화면이 깨질 때 복구 화면 표시 (오류 경계)
- 내부 개선
  - 타입 검사 도입 및 잠재 타입 버그 수정 (분석 패널 단어 저장 관련)
  - 자동 테스트 도입 (데이터 저장 로직 11건)
  - CI에 타입 검사·테스트 단계 추가 (릴리즈 빌드 전 자동 검증)

## v1.7.2
- 버그 수정

## v1.7.1
- 버그 수정

## v1.7.0
- **문법 노트** — 문법 포인트를 저장·관리하는 전용 탭 추가
  - 전체 노트 / 노래별 노트 보기
  - 노트 추가 버튼으로 직접 입력 (포인트, 설명, 예문)
  - 즐겨찾기, 수정, 삭제
  - 가사 분석 패널 문법 카드의 `+` 버튼으로 즉시 저장

## v1.6.0
- **가사 줄 분석 기능** — 가사 편집 화면에서 각 줄 우측 `?` 버튼을 누르면 Claude Sonnet이 번역·문법 분석을 인라인으로 표시
  - 직역 / 의역 (의역이 직역과 같으면 숨김)
  - 단어 카드: 단어·읽기·뜻·품사 표시, `+` 버튼으로 단어장에 즉시 저장
  - 문법 카드: 핵심 문법 포인트와 설명
  - 분석 결과는 로컬 캐시에 저장 — 같은 줄 재분석 시 API 재호출 없음
- **⚠ 가사 분석 베타** — AI가 생성하는 내용으로 부정확한 번역, 오류, 이상한 설명이 나올 수 있음

## v1.5.0
- **컬러 테마 추가** — 설정 화면에서 3가지 테마 중 선택 가능
  - 파스텔 (기본): 핑크 & 라벤더
  - 화이트: 무채색 기반의 깔끔한 라이트 테마
  - 다크: 차콜 계열의 깔끔한 다크 테마
- 선택한 테마는 앱 재시작 후에도 유지됨

## v1.4.1
- 코드 품질 개선 및 버그 수정 (내부 변경)
  - AI 응답이 비어있을 때 발생할 수 있는 크래시 수정
  - 모달 빠르게 닫을 때 React 경고 제거
  - CSS 색상 변수 체계 정비
  - 공용 `useEscapeKey` hook 추출, 중복 코드 제거

## v1.4.0
- **단어 발음 필드 추가** — 단어장에 히라가나 읽기(reading) 필드 추가, 단어 카드에 발음 표시
- **AI 자동 입력** — `Ctrl+E`로 단어 추가 시 발음(kuroshiro)과 뜻(Claude Haiku)이 자동으로 채워짐
  - 일본어가 아닌 텍스트는 자동 입력 없이 수동 입력
  - 조사 단독 선택 등 유효하지 않은 단어는 뜻 필드 비워두고 안내 힌트 표시
  - API 키가 없으면 발음만 자동 입력, 뜻은 수동 입력
- **단어 수정 기능** — 단어 카드의 ✎ 버튼으로 저장된 단어의 발음·뜻 수정 가능
- **낱말카드 발음 표시** — 낱말카드 뒷면에 히라가나 발음 추가
- **기존 단어 발음 자동 보완** — 이전에 저장된 단어(발음 필드 없음)를 불러올 때 kuroshiro로 발음을 자동으로 채워 DB에 저장

## v1.3.0
- **단축키로 단어 즉시 저장** — 가사 편집 화면에서 단어를 드래그 선택 후 `Ctrl+E`를 누르면 단어장 추가 모달이 열리며 선택한 단어가 자동으로 입력됨
- **선택 힌트 팝업** — 텍스트를 선택하면 단어 근처에 `Ctrl+E로 단어 추가` 안내 팝업이 자동으로 표시됨
- **단축키 튜토리얼 토스트** — 번역 화면에 처음 진입할 때 단축키 안내 메시지가 잠깐 표시됨 (이후 재표시 안 함)
- **단어 추가 모달 개선** — `Ctrl+E` 단축키나 단어 클릭으로 모달을 열면 해당 단어가 입력 필드에 미리 채워짐

## v1.2.0
- **한글 발음 토글 기능 추가** — Claude AI(Haiku)를 이용해 히라가나 발음을 한글 발음으로 변환
  - 가사 편집 화면 상단 `[ひ → 가]` 버튼으로 히라가나 ↔ 한글 발음 즉시 전환
  - 곡당 최초 1회만 API 호출, 이후 토글은 로컬 DB 캐시 사용 (추가 비용 없음)
  - Anthropic API 키는 기기에 암호화 저장 (`safeStorage`)
  - 히라가나 발음은 그대로 유지되어 학습 목적에 따라 선택 가능

## v1.1.4
- 릴리즈 업로드 권한 문제 수정

## v1.1.3
- GitHub Actions 릴리즈 워크플로우 오류 수정

## v1.1.2
- Windows / Mac 자동 빌드 및 릴리즈 배포 (GitHub Actions) 추가

## v1.1.1
- 앱 이름 수정
- 앱 아이콘 추가

## v1.1.0
- 저장된 노래의 제목, 아티스트, 가사, 히라가나 발음을 번역 화면에서 직접 수정 가능

## v1.0.1
- "다시 입력" 버튼 텍스트를 "새로 입력"으로 변경
- 새로 입력 시 제목·아티스트 초기화 버그 수정

## v1.0.0
- J-Pop 가사 붙여넣기 → 히라가나 자동 변환
- 줄별 번역 입력 및 저장
- 단어장 기능 (노래별 / 전체 보기, 즐겨찾기, 셔플, 낱말카드)

---

## 예정 기능
- 번역한 가사를 txt / PDF로 내보내기 (가사 · 발음 · 번역 3단 구성)
